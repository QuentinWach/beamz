# Named physics regressions

Each test here should name the historical failure, preserve the smallest
reproducing configuration, and assert the physical quantity that was previously
wrong. Avoid snapshots of internal arrays when an observable or invariant can
express the bug.
