+ Use the full 120 characters of a line if possible, both for the code but also for arguments of classes and defs.
+ Format if-statements into single lines if possible.
+ Avoid writing exceptions.
+ Don't specify types of variables arguments etc. and don't use modules for typing.
+ Only write single line descriptions in """ """ under functions rather than long argument, return explanations etc.
+ Don't use data classes.
+ Don't use decorators.